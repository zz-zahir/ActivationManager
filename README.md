# CTIBAuth
```
this php script manages the activated desktop application clients.
you simply need a similar code in your desktop application as in the activation.py code.

features
client side : the code send an activation key
server side:
  activate
  deactivate the key
  geolocalisation
  uploading and populating the database with xlsx file
  
there is an xlsx file to populate the database with samples
there is a sql file to populate the database with sample

enjoy
```
